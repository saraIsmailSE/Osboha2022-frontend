import { api } from "../../Intercepter"

export const rejectThesis = async (id) => {
 
    // const    thesis = await api.get(`thesises/user_book_id/${id}`);
    // (   thesis.data)
    // return    thesis.data.data;
console.log('test')
}


